# Status

_Last updated: 26 September 2026 (with release 1.26.0, awaiting merge)._

Mimar is a house-planning app for Pakistan (feet and marla, local plot sizes and construction), built as a
Windows desktop app (Electron) and a web app (GitHub Pages, installable, works offline) from the same code.

- Repository: `mbnaveed15-sys/mimar-app` (public, by the owner's choice).
- Web version: https://mbnaveed15-sys.github.io/mimar-app/ (published from `main` by `.github/workflows/pages.yml`).
- Windows builds: GitHub Releases, built by `.github/workflows/build-installer.yml` on every merge to `main`.
  The installed app updates itself from the latest release; the portable one offers the download.

## Releases

| Version | Main additions |
| --- | --- |
| 1.26.0 | Awaiting merge. Interface polish. **Top bar** fits a 1024 px window (Search and 2D/3D keep their icons and drop their words below 1280 px). **Tool keys** keep working after ticking a box or moving a slider in the side panel (a drop-down lets go once a choice is made); Ctrl+S, Ctrl+Shift+S, Ctrl+O, Ctrl+N, Ctrl+P and Ctrl+K work from inside a field (the field's value is taken first). **Unsaved changes** dialog: Save (the default: Enter), Don't save, Cancel (Esc or a click outside); Save carries on only once the plan is saved. **Plot**: a click inside it away from rooms picks it (its edge is often under the boundary wall); Plan check offers "Select the plot" when a plot has no bylaws yet. **Keyboard**: dialogs and the welcome/tour keep Tab inside and give focus back when they close; the tour takes ← and →; Esc from a menu leaves focus on its name (tool keys still work there) and ↓ opens it; the right-click and tool-bar menus take ↑ ↓ Home End; a tool-bar grip opens its menu with Enter. **Paste** (Ctrl+V or right-click) hangs the copy on the pointer by its bottom-left corner, snapping like Move; a click puts it down (one undo step) and Esc takes the paste back; Paste is left out of the right-click menu when nothing is copied. **Exports**: an empty plan (or floor) says so instead of saving a blank PDF, PNG or DXF |
| 1.25.0 | Released 26 Sep 2026. SketchUp-style 3D. **Camera:** zoom extents (0 / Shift+Z) and +/− work in 3D and keep the viewing direction; standard views (View menu and the 3D corner: Top, Front, Back, Left, Right, Iso) and a Parallel/Perspective switch; orbit turns about the point under the cursor; the camera never goes under the ground. **Snapping:** the 3D snap marker sits where the pointer hits (a wall top, say) with a dashed line down to the floor and a label by the cursor. **Select:** a drag always draws a box (even from on an item; Move is M); double-click takes a wall with its doors and windows, triple-click everything joined to it. **Tape** in 3D measures between points on items at their heights (the readout adds the height difference); ↑ measures straight up. **Push/Pull:** double-click repeats the last distance; a distance typed straight after a push changes it; the face snaps to other items' tops, bottoms and sides. **Walls have two sides:** each side can have its own material (Inspector: "Side facing Room 1", "Outside side"); in 3D, Paint paints only the face clicked (a side, or the top and ends); 2D paint still paints the whole wall; exports keep each side's material. **Rotate** shows a protractor (15° ticks, the angle swept) in 2D and 3D. **Speed:** materials and textures are cached across rebuilds and one renderer is reused (544-wall test plan: reopening 3D 2.15 → 1.59 s, a rebuild 1.2 → 0.74 s; first open and orbit unchanged) |
| 1.24.0 | Released 26 Sep 2026. AutoCAD-style 2D. **Snapping:** the nearest point wins (ends, wall-face corners, midpoints, crossings including wall faces, perpendicular from the last point, the axis meeting a wall); then the building line; then the red/green axis from the last point (ahead of grid points, so lines from off-grid points stay level); then along a wall, a wall face or a layout line at the grid step (the dead "on wall" reach fixed); then the grid. The Tape snaps to the building line. **Typed input:** `@x,y` and `length<angle` (y up, 0° right, counter-clockwise) in Wall, Layout line and Move; fractions of an inch; metric bare numbers are millimetres; Fillet radius 0. **AutoCAD aliases** typed quickly (CO/CP, RO, TR, EX, MI, SC, CHA, REC, DI, BR, FIL, STR; the first letter still picks its own tool; also in search and the shortcut list). **Enter** on the Select tool repeats the last tool; **right-click** while drawing finishes like Enter; **Ctrl+Z** in a wall/line chain takes back one piece and carries on. **Modify:** Move/Rotate keep the selection when the base point is on another item; Stretch/Scale/Rotate/Mirror snap to the plan as it was (no tilt from the preview); Stretch only touches the floor shown and nothing hidden or locked; Rotate turns counter-clockwise for a positive angle; doors keep their distance from the wall end that stays put; Offset, Trim, Extend, Break, Join, Fillet and Chamfer work on layout lines, which also cut and bound walls. **Speed:** the plan drawing and check marks are memoised and wall joins indexed (2,000 walls: hover 260 → 17 ms per move, draw a wall 9.5 s → 0.3 s, pan 330 → 83 ms) |
| 1.23.1 | Released 26 Sep 2026. Safety and correct results, from a six-tester QA pass (reports in the git-ignored `.qa/`; summary in `04-backlog.md`). **Data loss:** picking plan-check rows or hints goes to the items' floor first (Delete no longer erases an unseen floor); the file name and "unsaved" flag survive a reload (File › New asks); warnings when autosave fails (storage full) or another window is open; Break/Trim say when a door or window went with the cut; a wall split inside a component copy gets its own key (no duplicate ids). **Limits:** typed lengths over 1 km refused; damaged files can't load 100 km coordinates, 10 m+ stair rises, zero or negative widths, or control characters in names (these froze or crashed the app); PDFs too big for the paper are refused with a message. **Speed:** plan hints and the plan check run once per change, pause while dragging, and use a spatial index (hints on ~1,000 rooms 12 s → 60 ms); finding rooms from walls ~50× faster; DXF export no longer overflows on big plans. **Plan check:** covered area measured to the walls' outer faces with each wall once and without lawns/courtyards/driveways (it was ~10% high); "Guest bath", "Master bath" etc. are baths; a bylaw row is picked by size when size and frontage don't fit one row; LDA 1–2 kanal band from 20 marla; PDF check page columns no longer overlap, "⅓" prints, the page carries the plan's name; the room total in the side panel is "Room area". The sample house gets a lounge–kitchen door and a wider lounge window |
| 1.23.0 | Released 26 Sep 2026. Plan hints (good practice, not bylaws, shown in amber under Plan check with dotted marks on the rooms; click to select; Settings can turn them off or add them to the PDF): rooms reached through doors from the entrance (or, upstairs, from the stairs), rooms with no door, rooms reached only through a bedroom; living rooms and kitchens on an outside wall, with a window, and windows of at least a tenth of the floor; good-practice sizes by room name; the kitchen next to the dining room or lounge; a prayer room not opening onto a bath or kitchen. Bylaws: the **mumty** (a floor named Roof/Mumty, or with a room so named) is not counted as a storey; its area (CDA ⅓ or ¼ of the buildable block; DHA 8.57.5), height (CDA 10', DHA 11') and width (DHA half the plot) are checked, and CDA's 30' height leaves it out; **car porch** sizes for DHA Islamabad (8.56.10.1). A **north arrow** on the PDF, with "North points" in Settings (saved with the plan). The **building line snaps**: walls and rectangles snap half a wall in, columns half a column in, so their outer face touches the line (ahead of a grid point on it); rooms, slabs and layout lines snap onto it ("On building line") |
| 1.22.0 | Released 25 Sep 2026. Bylaws and plan check: a plot's authority (CDA sectors, Islamabad private schemes, DHA Islamabad/Rawalpindi official; DHA Lahore, Bahria Lahore, LDA setbacks provisional) fills its setbacks from the table by size and frontage, with side 1/side 2 and Swap; plot size presets place a plot in one click; a live Plan check section (setbacks, items in setbacks, projections, coverage, first floor, plinth, storeys, height, boundary wall, room sizes, total area) with clauses, red marks on the plan and click-to-select; bylaws in the PDF title block and a Plan check page (setting) |
| 1.21.0 | Released 25 Sep 2026. Dockable tool bars: each tool section is its own bar with a grip; drag it (or right-click the grip) to dock at the left, right, top or bottom, next to other bars; left/right columns wrap into more columns instead of scrolling; layout remembered; View › Reset tool bars |
| 1.20.0 | Released 25 Sep 2026. Push/Pull follow-ups: a shape on a wall pushed part-way in is a **niche** (at least 2" of wall stays behind; deeper cuts right through), pulled out it is a **projection** (chajja, ledge, pilaster); a Depth field sets either. **Push/Pull in the 2D plan** for faces seen from above (wall sides and ends, slab and block edges, column sides, beam sides and ends). Only the face a click would take is lit up (3D and 2D). Niches and projections in 2D (dashed), 3D, 3D exports and DXF (`A-WALL-PROJ`) |
| 1.19.0 | Released 25 Sep 2026. Push/Pull (P) in 3D: wall tops (height), ends (length), sides (thickness); slab tops, bottoms and edges; column tops and sides; beam depth, width and length; blocks. Shape tool (Shift+R): rectangle, circle, arch or polygon on the floor, a slab or block top, or (in 3D) a wall face. Pull a floor shape up into a **block** (new item, own layer); push a wall shape half-way in to **cut an opening** of that shape (rectangular, round, arched or any outline; open or glazed); push a slab shape down through to make a **void**. Shaped openings are built as wall panels in 3D and exported (GLB/DAE/OBJ); voids and blocks go to DXF. Windows get a height field and "Open (no glass)"; walls and columns get their own height. Plot moved to Shift+P. Grid: minor and major sizes on sliders with stops (major as a length, a whole number of minor squares) |
| 1.18.0 | Released 25 Sep 2026. Plots above the grid (grass tint in 2D; grid as a ground-floor backdrop under a slightly see-through lawn in 3D); "Height above floor" for walls, columns, beams, slabs, stairs, furniture and layout lines (negative for sunken); window sill height; Move in 3D with ↑/↓ locks to the blue axis (pointer or typed height); Alt+↑/↓ raises or lowers by a grid step (Shift: a fifth); "+2' 0"" tags in 2D; Electron's default menu bar removed |
| 1.17.0 | Released 25 Sep 2026. First-run welcome and 6-step tour, sample 5-marla house, extruded-M logo everywhere, layout (drafting) lines with "Turn into walls", automatic layers by item type with hide/lock per layer and per item, drag-to-erase, grid points snap even under walls, room names drawn over furniture |
| 1.16.1 | Fixed a crash on mixed selections (columns, beams, slabs, plots, stairs); 3D selection box on screen; horizon guard; square-on 3D camera for empty plans; smoke test over every item type |
| 1.16.0 | Build in 3D: every tool works in the 3D view (raycast to the floor), SketchUp-style camera (middle-drag orbit, Shift+middle pan, wheel zoom, Orbit tool O), 3D picking, previews and snap marker |
| 1.15.0 | Grid colours per theme; touch (pinch, two-finger pan, long-press menu) and pen; web version (PWA, offline, Pages) |
| 1.14.0 | Materials library (63, 8 collections, search, textures in 3D); DXF, GLB, DAE, OBJ export |
| 1.13.0 | Plot with setbacks, boundary and parapet walls, gates, stairs maker (straight, L, U, ramp) |
| 1.12.0 | Floors (levels), plinth, columns, beams, slabs; stacked 3D |
| 1.10–1.11 | Selection, groups and components; Modify tools (offset, mirror, trim, extend, break, join, fillet, chamfer, stretch, scale) |
| earlier | 2D drawing, rooms and marla areas, doors and windows, furniture, PDF/PNG export, themes, auto-update |

## Checks at 1.26.0

292 unit tests (Vitest) and 60 browser tests (Playwright) pass; `npm run check` (types, lint, format, unit tests,
build) is green.

## Things the owner has decided

- Keep the repository **public** (making it private would break free updates and Pages).
- **No paid services.**
- Logo: the **extruded M** (chosen over a T-square set and other options). `scripts/logo.mjs` redraws every logo file.
- Walls: a **click** continues into the next wall; a **drag** makes one wall and stops.
- Camera in 3D: **SketchUp style** (option A).
- Old releases: the owner clears them by hand (no automatic clean-up).
